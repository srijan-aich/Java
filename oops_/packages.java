/* I am not gonna use packages because I already have a folder setup
if I use packages it would make things messy, but I do know packages */

// packages tools; creates tools folder
// if we put files in tools folder then we can use -  import.tools.filename;   to access those files
// all files - import tools.*;

//packages can have unique names, so that we can avoid name conflicts
// package com.example.myapp;  // this is a unique name for a package

